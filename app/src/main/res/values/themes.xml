<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EvaLeadCapture" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorAccent">@color/primary_color</item>
    </style>

    <style name="Theme.EvaLeadCapture" parent="Base.Theme.EvaLeadCapture" />
    <style name="NoActiveIndicatorStyle">
        <item name="android:color">@android:color/transparent</item>
        <item name="android:alpha">0.0</item>
    </style>
</resources>